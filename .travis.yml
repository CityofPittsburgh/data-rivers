language: python
python:
  - "2.7"
cache: pip
install:
  - pip install -r requirements.txt
script:
  cd airflow_scripts/dags/dependencies/dataflow_scripts && python -m unittest discover tests
git:
  quiet: true